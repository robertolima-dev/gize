# Runtime configuration for chat (copy to .env)
DATABASE_URL=sqlite://chat.db?mode=rwc
PORT=8080
# Secret used to sign auth tokens (JWT/HS256). Use a long random value in production.
GIZE_JWT_SECRET=dev-only-change-me
